spring:
  datasource:
    url: jdbc:mysql://database-1-karental.cbsocu2qk2qz.ap-southeast-1.rds.amazonaws.com:3306/karental
    driverClassName: com.mysql.cj.jdbc.Driver
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST}
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL_USERNAME}
    password: ${EMAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
front-end:
  domain-name: ${FE_DOMAIN_NAME}
payment:
  vnpay:
    return-url: ${VNPAY_RETURN_URL}